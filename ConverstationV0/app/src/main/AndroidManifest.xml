<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="edu.stanford.converstationv0">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConverStationInfo"></activity>
        <activity android:name=".ConverStationProfile"></activity>
        <activity android:name=".ArrivedAtGroupConversation" />
        <activity android:name=".NavigationSteps" />
        <activity android:name=".CreateProfileActivity"></activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".EditProfileActivity" />
        <activity android:name=".MyProfileActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".GroupArrivedActivity" />
        <activity android:name=".GroupNavigationActivity" />
        <activity android:name=".GroupMembersActivity" />
        <activity android:name=".GroupConversationActivity" />
        <activity android:name=".ConverStationInfoActivity" />
        <activity android:name=".GroupMapActivity" />
        <activity android:name=".IndividualConversationActivity" />
        <activity android:name=".IndividualArrivedActivity" />
        <activity android:name=".LocationInfoActivity" />
        <activity android:name=".IndividualNavigationActivity" />
        <activity android:name=".FoundSomeoneActivity" />
        <activity android:name=".PartnerProfile" />
        <activity android:name=".waiting" />
        <activity android:name=".AddTimeActivity" />
        <activity android:name=".PhotoPickerActivity" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".schedule"
            android:label="@string/title_activity_schedule"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".WelcomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>